apiVersion: v1
kind: Service
metadata:
  name: orchestration-service
spec:
  type: NodePort
  selector:
    app: orchestration-service
  ports:
    - port: 8082
      targetPort: 8082
      nodePort: 30082